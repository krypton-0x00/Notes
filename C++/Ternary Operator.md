
```cpp
	if(level > 4){
		s_speed = 5;
	}else {
		s_speed = 2;
	}
	//using ternary
	s_Speed = level > 5 ? 5:2;
//we Can also nest the ternary operator;
s_Speed = (s_level > 5)? (S_level>10? 15: 10) : 5 //remove the brackets i add them to see a little be clear;

```